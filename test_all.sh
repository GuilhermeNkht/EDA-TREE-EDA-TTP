#!/bin/bash

FILE="instances/NL12.txt"
ELITE=0.5
RETAINED=0.75
MAX_EVA=1000000
N_POP=1000
NUM_RUNS=30
SEED=2026
NAME="RANDOM"

(cd ttp && ./bin/ttp_opt "$FILE" "$ELITE" "$RETAINED" "$MAX_EVA" "$N_POP" "$NUM_RUNS" "$SEED" "$NAME")